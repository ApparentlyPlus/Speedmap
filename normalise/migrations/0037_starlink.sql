-- The register lists who files coverage in Greece, and a satellite constellation files
-- none: it reaches everywhere and so appears nowhere. It is still the only answer at the
-- addresses no line reaches, which is exactly where a coverage map has least to say.
-- No register_id, because it has none to have.
insert into provider (code, display_name, kind, builds_own_network) values
    ('STARLINK', 'Starlink', 'satellite', true);
